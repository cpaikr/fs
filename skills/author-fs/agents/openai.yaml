interface:
  display_name: "Author FS Documents"
  short_description: "Resolve source statements, then encode or repair FS"
  default_prompt: "Use $author-fs to resolve these financial source materials when needed, then encode or repair them as a conforming FS document."
