{
  "id": "create-malformed-json",
  "command": "fs",
  "arguments": ["create", "candidate.json", "--output", "result.json"],
  "workspace": [
    { "copy": "../raw-input/malformed-json.json.txt", "to": "candidate.json" }
  ],
  "stdin": null,
  "expect": {
    "stdout": {
      "encoding": "json",
      "members": [
        { "pointer": "", "equals": ["error", "help"] },
        { "pointer": "/error", "equals": ["operation", "code", "message", "path"] }
      ],
      "equalities": [
        { "pointer": "/error/operation", "equals": "create" },
        { "pointer": "/error/code", "equals": "invalid-json" },
        { "pointer": "/error/path", "equals": "candidate.json" },
        { "pointer": "/help", "equals": [] }
      ],
      "nonemptyStrings": ["/error/message"]
    },
    "stderr": { "encoding": "bytes", "equalsUtf8": "" },
    "exitCode": 1,
    "filesystem": {
      "workspace": {
        "created": [],
        "unchanged": ["candidate.json"],
        "absent": ["result.json"]
      },
      "home": { "created": [], "unchanged": [], "absent": [] }
    }
  }
}
